import { CalendarMaskDirective } from './calendar-mask.directive';

describe('CalendarMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarMaskDirective();
    expect(directive).toBeTruthy();
  });
});
