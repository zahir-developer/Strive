import { NavigatePageDirective } from './navigate-page.directive';

describe('NavigatePageDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigatePageDirective();
    expect(directive).toBeTruthy();
  });
});
